export enum Conflict {
    INVALID_FIELDS = "Some fields are invalid",
    ALREADY_EXISTS = "This resource already exists",
    NOT_FOUND = "This page doesn't seem to exist",
    DISABLED = "This section is disabled",
    UNAUTHORIZED = "The authorization was invalid"
}